import "./globals.css";

export const metadata = {
  title: "IoT Cloud Admin",
  description: "Cloud Metadata Admin Dashboard",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="bg-black text-white antialiased">
        {children}
      </body>
    </html>
  );
}